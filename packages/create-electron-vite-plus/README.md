# create-electron-vite-plus

Create an Electron application powered by `electron-vite-plus` and Vite+.

```bash
npm create electron-vite-plus@alpha my-app -- --template react
cd my-app
vp install
vp run dev
```

Available templates: `vanilla`, `react`, and `vue`.
